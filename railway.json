{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd client && rm -rf node_modules dist && npm install --legacy-peer-deps && npm run build && cd ../server && rm -rf node_modules && npm install",
    "watchPatterns": ["client/src/**", "server/**", "client/package*.json"]
  },
  "deploy": {
    "startCommand": "node server/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
